const Navbar = () => {
    return (
        <nav className="flex justify-between px-5 py-3 bg-blue-700 text-white">
            <div>TopBoard</div>
            <div>Blank</div>
        </nav>
    );
};

export default Navbar;
